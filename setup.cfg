# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
versionfile_source = t_rax/_version.py
versionfile_build = t_rax/_version.py
tag_prefix = ''
parentdir_prefix = ''

[coverage:run]
omit = _version.py